#!/usr/bin/python3
import socket
import select
import threading

hote = "localhost"
port = 7777

socket = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
socket.connect((hote, port))hote = "localhost"

def print:
    
while(1):
    
    if(
    
socket.close()
